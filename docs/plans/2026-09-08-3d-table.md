# M3 — 3D table (apps/table)

> Status: **ACTIVE — user approved 2026-09-08 ("implement m3 as designed"); P1 shipped+reviewed, P2 next.** Milestone: a playable, cinematic
> browser table for catan-vtt that connects to the shipped M2 room server over
> WebSocket. Visual components are delegated per user routing (hy4-preview
> primary). Mirrors mahjong-vtt/dnd-vtt apps/table pattern.

## Goal

`npm run dev` at `apps/table` (Vite :4274) renders the Catan island in 3D and
lets 3–4 friends play a full game with mouse/touch — using ONLY the M2 wire
protocol (join/op/projection/event; no kernel imports for legality; the
projection is the entire truth of the client).

## Design rulings (locked at plan time)

1. **Stack**: **React 19 + Vite + three.js via @react-three/fiber v9 + drei** —
   pinned to the proven sibling combo (dnd-vtt/mahjong-vtt apps/table:
   react@^19.2.8, three@^0.185.1, fiber@^9.7.0, drei@^10.7.8, vite@^8,
   @vitejs/plugin-react@^6). fiber v9 peer-requires React 19; React 18 is the
   WRONG pin (caught live during P1 install, 2026-09-08). UI chrome (HUD, lobby,
   modals) = plain React/CSS overlay, not in-canvas.
2. **Renderer is a pure client of the wire.** It never imports `applyAction` or
   `legalMoves` from the kernel; `legalMoves` arrives on the projection
   (protocol.ts). Redaction is server-enforced — the table physically cannot
   show an opponent's hand. (Client rule already documented in `redact.ts`.)
3. **Geometry is procedural + data-driven from the projection**: hex board from
   `board.ports/hexes`, vertex ids for settlements/cities, edge ids for roads.
   No hand-authored board JSON — the kernel owns the map.
4. **Click → op mapping**: UI enables ONLY what the current projection's
   legalMoves allows; every interaction resolves to exactly one Op and goes
   through `op{...}`. Optimistic UI: none — wait for `event{opApplied}` (the
   room is the single source of sequencing).
5. **Assets**: CC0/MIT/primitive-materials only; no Catan GmbH art (hard rule).
   Stylized low-poly: colored hex tops + procedural terrain shading, tokens as
   primitives (houses/cities/robber as simple meshes), dice as rounded cubes.
6. **Sessions**: table talks to `ws://localhost:4273` (env `VITE_ROOM_URL`);
   lobby screen = enter roomCode + name + seat; rejoin = seatToken in
   localStorage (rotation-aware: save the newest token from every welcome).
7. **Testing**: vitest + happy-dom for non-render logic (projection→scene
   adapters, op-intent builders); visual proof = Playwright screenshots of the
   running table under `docs/e2e-review/m3-*` (browser harness can't screenshot
   WebGL headlessly — same constraint as siblings; use the established sibling
   workaround if needed).

## Phases

- **P1 scaffold** (~1 wave): `apps/table` Vite+R3F app, wire client
  (`useRoom()` hook: connect/join/receive/parse ServerMsgSchema, rejoin token),
  hex-board scene from projection, static camera. AC: board renders from a live
  seeded room; typecheck+test green.
  **DONE 2026-09-09** (`1fd76d3`,`dec1a8d`,`fd0f205`,`6186820`): 337/337 +
  vite@8 build + browser screenshot of live spectator game
  (`docs/e2e-review/m3-p1/`). Token STORAGE ships (adapter
  store/read/clearSeatToken + disconnect-forget); the auto-rejoin-on-load call
  site is deferred to P2 with the HUD. SPEC REVIEW (parent-conducted after
  3 reviewer ceiling-deaths): PASS — AC3 src grep type-only-clean (runtime
  schema imports are WIRE envelopes, explicitly allowed); Q1 ruling: kernel
  imports in TESTS are the contract check, not a violation; join-once holds
  (connect returns false when live); OFL text bundled `d2b4a97`.
- **P2 interaction**: HUD (turn banner, your resources, legalMoves menu),
  vertex/edge click → placement ops, dice-roll button, seven-window discard
  modal, trade + dev-card panels, robber drag-to-hex. AC: full scripted game
  playable by clicking (demo: 3 browser tabs or Playwright-driven).
  **P2(a) DONE — double-gated** (DOM half + refresh auto-rejoin): HUD/ResourceRail/
  TradePanel/DevCardPanel/MovesList-enabled shipped `100e233`; browser smoke proves a
  click advanced serverSeq 4→5 and reload auto-rejoined (docs/e2e-review/m3-p2a/).
  Quality review `deleg_85447e1a` CHANGES-REQUESTED 0C/5I/9M → all dispositioned
  parent-side, `f8d17e2`: trade selects list ONLY shipped pairs (I-1); offer
  affordance state-derived, kernel never enumerates composed tradeOffer by design
  (I-2); StrictMode rejoin latch removed (I-3, rf-03-rejoined.png proof); dev-card
  pickers from shipped ops (I-4); render-derived select reconciliation (I-5);
  minors incl. lastRoom seat clamp + favicon 404. Root 356/356, re-smoke green,
  zero true remote requests (blob: rasters were a probe-filter artifact).
  Spec pass: parent-conducted (AC3 grep clean; gating now shipped-only by I-1/I-4).
- **P2(b1) DONE, double-gated** (8a15665 + quality 374fa8c): click THE ISLAND.
  targetSet.ts pure projection (ghost exists iff server shipped that id; ops
  identity-equal) + Targets.tsx interactive vertex/edge/hex ghosts (hover,
  clamp-to-live I-1 fix) + DiscardModal (combinations validated against the
  server's shipped discardSeven list, sent verbatim) + mayActOnBoard gating
  (roller-gate CONFIRMED vs turn.ts by reviewer). Browser proof x2: 47 ghosts,
  canvas click -> serverSeq 4->5, rejected=false, orbit-drag no-op, cursor
  asserts green, 0 page errors. 372/372. Spec pass parent-conducted: ACs for
  the b1 slice green (clicks project + send shipped ops; AC3 grep clean;
  discard modal unit-proven).
  **P2(b2) open**: robber steal-flow polish (victim DOM picker).
- **P3(a) DONE, double-gated** (57a3934 + quality 3bfa266): victory banner,
  pure client derivation from the projection's PUBLIC winner/finalPoints —
  the planned gameOver wire frame was PROBED AND REJECTED (the projection
  field can never be missed; the frame would have been a cross-package
  duplicate with zero gain; reasoning in docs/plans/2026-09-09-m3-p3a-victory-
  overlay.md). Redaction honesty is structural (VictoryInput param, no cast
  escape; child mutation-tested 8 ways). Quality APPROVED 0 Critical; I-2
  stale-banner-across-disconnect/cross-room fixed root-cause (adapter welcome
  clears projection) + belt (status gate) with a browser regression (Leave
  hides banner); I-1 opLabel.test.ts (7) covers eventTail incl. the
  unreachable-today null-winner defense. 396/396.
  **P3(b) DONE, double-gated** (a5a72ee + quality 97a8c50): rematch on the wire
  + the host button. Host = seat 0, server-enforced; honest refusals
  notHost/badPhase; server-derived seed (wireScrub law); #endedSent resets so
  game 2 gets its own gameEnded (M2 review-9 debt). Review 0C/2I: the sticky
  refusal note (a transient badPhase survived into a whole fresh game) fixed
  in the adapter projection case + owner-guard/authority pins. 421/421.
- **P3(c) DONE — AC2 PASSES** (597c2ad, f708779, 48bb370, 629ba6b, 12625e1):
  a full 3-tab browser game to a REAL 10-VP winner. 606 ops / 1.9 min /
  seq 622 on all tabs / REJECTED 0 / pageerrors 0; domestic trade 34 offers →
  20 accepts; rematch clicked → all tabs phase=setup, banner gone, board empty
  (05-victory.png + 06-rematch-fresh.png, both vision-verified). The demo also
  caught a real deadlock: the discard picker was SET-based, so a {wood:2,
  ore:6} hand owing 4 had no reachable selection — now a multiset
  (hudLogic.toggleDiscardPick, pure + 6 tests). catan:e2eWin never set.
  **P3(d) open**: cinematic polish (camera fly, dice tumble, build
  animations), spectator view.
- **P3 (expanded)**: everything under P2(b2)/P3 above, plus cinematic polish —
  camera fly (turn handoff), build/dissolve animations, dice tumble, sound-off
  by default, spectator view (seat:null → bird's-eye, no HUD actions).
- **P4 evidence + docs**: Playwright stills for the ACs, FUNCTIONAL-SPECIFICATIONS
  §table, STATE-SNAPSHOT, plan → done.

## Acceptance criteria

- AC1: `npm run dev` :4274; join a live room server :4273 with roomCode+name.
- AC2: Full 3p game playable end-to-end through the table UI to a 10-VP winner
  (Playwright-driven or 3 manual tabs with captured evidence).
- AC3: Client imports ZERO rule logic (grep proof: no applyAction/legalMoves/
  redactForSeat in apps/table); all legality comes from shipped legalMoves.
- AC4: Rejoin after refresh restores seat + seatToken rotation honored (old tab
  fenced; matches M2 lifecycle tests).
- AC5: Spectator mode renders read-only island with broadcast events.
- AC6: No copyrighted assets: all meshes/materials procedural or CC0.

## Out of scope

Mobile layout pass, account/auth, persistence/DB, Railway deploy (user:
"more mature, then we deploy"), seeds beyond base game, voice/chat UX.

## Delegation shape

P1: 1 build agent (scaffold + wire client + board scene). Review wave:
spec + quality (READ-ONLY). P2 split by surface: (a) HUD+menus+modals,
(b) 3D interaction (clicks/drag/robber). Each gets its own 2-stage review.
