# catan-vtt — Session Recaps

Drop files here as `SESSION-RECAP-YYYY-MM-DD.md` at the end of each working session (see the `write-session-recap` skill). The plan → build → recap → wrapup cycle expects one per session that touched anything non-trivial.
