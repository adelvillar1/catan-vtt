# State Snapshot

> Replace (don't append) from live data at each refresh. Stat tables live HERE, not in topical docs.

- **2026-09-09 (M3 P1 LANDED, review-fix batch shipped):** `apps/table` = Vite 8 + React 19 + R3F 9.7 + drei 10.7 + three 0.185 (sibling-proven pins; a react@18 attempt ERESOLVE'd — fiber v9 peer-requires 19). Wire client `useRoom` (parse-or-die; StrictMode-safe — send outside setRoom updaters, ref is truth) + strict `adapter` + procedural scene (`geom.ts` coordinate-frame proof; Island/Buildings/Roads/Ports derived from the projection ONLY) + DOM overlay (JoinPanel / read-only MovesList / EventTicker). Rubik (OFL) bundled → drei Text's jsdelivr CDN fetch eliminated; browser capture asserts zero remote requests. FIRST BROWSER EVIDENCE: Playwright-core (borrowed from mahjong-vtt node_modules) + cached Chromium (SwiftShader WebGL) → `docs/e2e-review/m3-p1/rf-02-spectator-island.png`: 19-hex island, number tokens, port labels, seat-colored buildings/roads on kernel vertices, spectator `seq 602`. A screenshot caught a join-breaking bug (JoinPanel uppercased mixed-case room codes). Quality review: 0 Critical/8 Important/9 Minor, all dispositioned (lesson: vacuous empty-fixture test → layout.test now drives the FULL kernel setup via applyAction, test-only; AC3 src stays purity-clean). Two build children died at the 30-min delegation ceiling AFTER writing files — salvage-and-verify beats re-dispatch-from-scratch. `npm test` → **337/337** (18 files; table 89); root `npm run typecheck` covers 3 projects; vite@8 build green. Commits `1fd76d3` → `6186820`, spec PASS (parent-conducted; reviewer deaths on suite reruns) + OFL text `d2b4a97`.
- **2026-09-09 (M3 P2(a) DONE — table PLAYABLE from browser):** HUD/ResourceRail/TradePanel/DevCardPanel + enabled MovesList + auto-rejoin (seatToken+lastRoom). Browser proof (`docs/e2e-review/m3-p2a/`): with bots parked on seats 0+1, the browser's click advanced serverSeq 4→5 — only it could move the game — and reload auto-rejoined seat 2. Child died at ceiling after code, parent wrote tests + evidence (354→356). Quality review completed (14 min, read-budget prompt works): 0C/5I/9M, all fixed `f8d17e2` — trade/dev selects list ONLY shipped pairs; offer affordance state-derived (kernel never enumerates composed tradeOffer); StrictMode rejoin latch removed (smoke passed WITH the bug — lifecycle bugs need mechanism proof). `npm test` → **356/356**. P2(b1) (3D click targets + discard modal) in flight `deleg_514348b0`.
- **2026-09-08 (M2 COMPLETE, phases 1–4):** room core + protocol + WS transport (`apps/room/src/server.ts`, double-reviewed: spec ghost-seat/gameEnded + quality one-join-per-socket/release-every-seat/name-on-wire) + **phase 4 human path**: `cli.ts` (text REPL + `--auto` bot, strict-parse, acts only on shipped legalMoves) / `server-main.ts` (`npm start` :4273) / `demo/demo.ts` (N real child clients, unanimous-winner assert). Seeded 3-client wire game → `WINNER seat 2, 10 VP`, 0 rejections/602 ops; evidence `docs/e2e-review/m2-two-terminal/`; commits `708f4d2`→`571c628`. `npm test` → **248/248** (kernel 170 + protocol 20 + room 34 + server 24); tsx devDep added.
- **2026-09-08 (M1 close):** kernel `packages/shared` **0.5.0** — waves 1–5 all reviewed + committed. `npm test` → **170/170** (12 files — `it()` counts: rng 12, board 18, state 14, setup 11, turn 28, road 15, actions 7, trade 13, devcards 17, golden 10, redact 11, vp 8; vitest reports 170 incl. parameterized cases), `tsc --noEmit` clean, `npm run typecheck` clean. Commits `3465083`→`7e8d457` on `develop`, pushed (public GitHub `adelvillar1/catan-vtt`). Plan `2026-09-08-rules-kernel` → **done**; AC1–AC10 ✅ (matrix: `docs/features/rules-kernel.md`, 20/20 ops named).
- **Kernel facts:** 20 ops in `OpSchema` (single source of truth for the M2 relay whitelist); 32-code `ActionError` union; RNG contract `Rng.restore({rngSeed,rngCursor})`, roll=2/steal=1 draws, all other ops zero; setup generators property-tested over 10k seeds; golden bots win real 10-VP games (6/6 seeds); `redactForSeat` leakage-proof + 8-op optimistic-UI commutativity.
- **npm quirk on this machine:** global config sets `omit=dev`; project `.npmrc` (`include=dev`) cancels it. Root scripts call workspace binaries directly — nested `npm run` inside an npm script hangs here (docs/TROUBLESHOOTING.md).
- **Delegation pin:** `hy4-preview` (workbuddy proxy :8787) — user's PRIMARY; kimi k3 (:10100) hit a 5h quota window; GLM-5.2 (:8787) verified spare. 30-min ceiling, split/steer/surgical-finish as needed.
- **Copyright guard:** rulebook text/art must never enter the repo (hard rule in CLAUDE.md); the `catan-board-game` skill is the rules authority (patched with this session's corrected rulings).
- **2026-09-09 (M3 P2(b1) DONE, double-gated):** Click THE ISLAND — targetSet.ts (pure ghost projection from
  shipped legalMoves + discardCombinationKeys single-source) + Targets.tsx (interactive vertex/edge/hex ghosts,
  hover clamp, localStorage catan:e2eTargets hook) + DiscardModal.tsx (combination-legality picker) + App
  mayActOnBoard gating. Browser proof run TWICE post-gating (47 ghosts, canvas click seq 4->5, no rejection,
  orbit drag no-op, cursor asserts green). Quality review deleg_03bb0347: 2 Important (I-1 cursor leak via
  R3F removeInteractivity-no-onPointerOut — fixed + proven; I-2 vacuous-break drive — anti-vacuity flag;
  mayActOnBoard roller-gate CONFIRMED vs turn.ts), 5 minors incl. hex-ghost 30-degree alignment (reviewer's
  yaw value was wrong — recomputed from Hex.tsx, PI/6 is correct). 372/372, typecheck 0, build 266ms.
  Commits 8a15665 + 374fa8c + 0101100.
- **Discard-modal BROWSER proof deferred to P3** (six stack attempts, no seat-2 seven in script budget; logic
  unit-covered via pure helpers + kernel-exact multiset count). 03-discard.png deleted as misleading.
- Next refresh: M3 P3 (robber steal-flow polish, full-game demo setup->10VP incl. discard-modal pixels,
  rematch + victory screen).
