{
  "name": "@catan-vtt/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CATAN rules kernel — pure, seeded, applyAction-only. Single source of truth.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^2.1.9"
  }
}
