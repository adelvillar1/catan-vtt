# This machine's global npm config sets omit=dev, which silently skips
# devDependencies (vitest, typescript) on a fresh install. include=dev
# cancels that omission for this project. include-workspace-root lets the
# root run workspace binaries (vitest) via `npm test`.
include=dev
include-workspace-root=true
